#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer in question
 * Return: the abs of an integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
